
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.0
  creationTimestamp: null
  name: applications.kube-applier.io
spec:
  group: kube-applier.io
  names:
    kind: Application
    listKind: ApplicationList
    plural: applications
    shortNames:
    - app
    - apps
    singular: application
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Application is the Schema for the Applications API of kube-applier.
          An Application is defined as a namespace associated with a path in a remote
          git repository where kubernetes configuration is stored.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ApplicationSpec defines the desired state of Application
            properties:
              dryRun:
                default: false
                description: DryRun enables the dry-run flag when applying this application.
                type: boolean
              prune:
                default: true
                description: Prune determines whether pruning is enabled for this
                  application.
                type: boolean
              pruneBlacklist:
                description: PruneBlacklist can be used to specify a list of resources
                  that are exempt from pruning.
                items:
                  type: string
                type: array
              pruneClusterResources:
                default: false
                description: PruneClusterResources determines whether pruning is enabled
                  for cluster resources, as part of this Application.
                type: boolean
              repositoryName:
                description: RepositoryName defines a Repository resource from which
                  configuration is pulled for this Application.
                type: string
              repositoryPath:
                description: RepositoryPath defines the relative path inside the Repository
                  where the configuration for this Application is stored.
                type: string
              runInterval:
                default: 3600
                description: RunInterval determines how often this application is
                  applied in seconds.
                type: integer
              serverSideApply:
                default: false
                description: ServerSideApply determines whether the server-side apply
                  flag is enabled for this Application.
                type: boolean
            required:
            - repositoryPath
            type: object
          status:
            description: ApplicationStatus defines the observed state of Application
            properties:
              lastRun:
                description: LastRun contains the last apply run's information.
                nullable: true
                properties:
                  commit:
                    description: Commit is the git commit hash on which this apply
                      run operated.
                    type: string
                  finished:
                    description: Finished is the time that the apply run finished.
                    format: date-time
                    type: string
                  started:
                    description: Started is the time that the apply run started.
                    format: date-time
                    type: string
                  success:
                    description: Success denotes whether the apply run was successful
                      or not.
                    type: boolean
                  type:
                    default: unknown
                    description: Type is a short description of the kind of apply
                      run that was attempted.
                    type: string
                required:
                - commit
                - finished
                - started
                - success
                - type
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
